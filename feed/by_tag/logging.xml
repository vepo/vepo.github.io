<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="https://blog.vepo.dev/feed/by_tag/logging.xml" rel="self" type="application/atom+xml" /><link href="https://blog.vepo.dev/" rel="alternate" type="text/html" /><updated>2026-02-21T19:42:20+00:00</updated><id>https://blog.vepo.dev/feed/by_tag/logging.xml</id><title type="html">vepo</title><subtitle>Um repositório para todos os posts, palestras e tutoriais que já fiz. Java, Desenvolvimento de Software e reflexões sobre filosofia</subtitle><author><name>{&quot;twitter&quot;=&gt;&quot;vepo&quot;, &quot;linkedin&quot;=&gt;&quot;https://www.linkedin.com/in/victorosorio/&quot;, &quot;picture&quot;=&gt;&quot;/assets/images/me.avif&quot;}</name></author><entry><title type="html">Deixando rastros, ou como usar log</title><link href="https://blog.vepo.dev/posts/deixando-rastros-ou-como-usar-log" rel="alternate" type="text/html" title="Deixando rastros, ou como usar log" /><published>2020-05-14T00:00:00+00:00</published><updated>2020-05-14T00:00:00+00:00</updated><id>https://blog.vepo.dev/posts/15-46-54-deixando-rastros-ou-como-usar-log</id><content type="html" xml:base="https://blog.vepo.dev/posts/deixando-rastros-ou-como-usar-log"><![CDATA[<p><em>Esse post é necessário para outro que vou escrever….</em> 🤔</p>

<p>Log é uma ferramenta muito importante. Se você é apenas um desenvolvedor, talvez não saiba a utilidade dele, mas com certeza que cuida da operação do seu software sabe.</p>

<p>Com esse post, quero mostra:</p>
<ol>
  <li>O que é um log?</li>
  <li>Porque usar log em Java?</li>
  <li>Como usar SLF4J em Java?</li>
  <li>Apresentar um simples benchmark entre implementações do SLF4J.</li>
</ol>

<h1 id="formato-de-arquivo">Formato de Arquivo</h1>

<p>Pra ser bem estrito ao nome <em>log</em> significa registro. E esse tipo de arquivo não é necessariamente associado a um conteúdo. Há inuméros outros tipos de log, como por exemplo no Apache Kafka, cada <a href="https://kafka.apache.org/documentation/#intro_topics">partition</a> é um arquivo de log, há base de dados que funcionam basicamente como um log de operações, acredito que o <a href="https://www.datomic.com/">Datomic</a> é assim.</p>

<p>Na realidade, um arquivo de log é um arquivo que conterá registros de algo, que será sempre adicionado ao final do arquivo.</p>

<p>Nesse post vamos tratar de logs de execução. Esses são essenciais para qualquer aplicação.</p>

<h1 id="porque-sua-aplicação-deve-ter-log">Porque sua aplicação deve ter log</h1>

<p>Existem aplicações, servidores, <a href="https://www.researchgate.net/publication/323988067_Intelligent_Agents_and_Environment">agentes</a> e scripts. A diferença entre esses tipos de software está no modo como eles são executados.</p>

<h2 id="script">Script</h2>
<p>No caso de script, a execução desse sempre será supervisionada por um desenvolvedor, ou alguém de suporte. Normalmente é executado pela linha de comando e as informações devem ser mostradas no console. Nesse caso não é necessário um biblioteca de log, a eficiência não é um problema.</p>

<h2 id="aplicações">Aplicações</h2>
<p>Aplicações é um que temos instalado no nosso computador ou celular. Quando um erro acontece na aplicação tempos duas coisas:</p>
<ol>
  <li>A descrição do erro por um usuário mal-humorado</li>
  <li>Um usuário mal-humorado</li>
</ol>

<p>Nesse caso, muitas vezes o que o usuário irá descrever pode não ser suficiente para se compreender o erro. Se esse erro ocorrer com um aplicativo de celular… você tem uma legião de usuários mal-humarados e só… Como corrigir o erro?</p>

<p>Nesse caso o log deverá descrever o estado da aplicação e será bastante útil para um troubleshooting simples ou a correção do bug.</p>

<h2 id="agentes-e-servidores">Agentes e Servidores</h2>

<p>Ahhh… E se você não tem um usuário? Vai dar erro, ninguém vai ver… Você não vai saber como e onde deu o erro… Como faz?</p>

<p>Primeiro uma definição, Agentes são softwares inteligentes que são autonomos. Eles não precisam de interação com outros softwares/usuários. Você pode ver um em ação… <a href="https://diolinux.com.br/2019/07/google-abre-codigo-de-seu-web-crawler.html">o google</a>!</p>

<p>Nesse caso, um log de execução é essencial. Você precisa saber o estado atual do seu sistema e se der algum erro, deve ter informações suficientes para compreender o que aconteceu.</p>

<h1 id="uma-biblioteca-para-todos-usarem">Uma biblioteca para todos usarem</h1>

<p>No mundo Java há uma grande preocupação com padronização. Assim até se tratando de Log também temos uma padrão:</p>

<ol>
  <li>Não use a biblioteca padrão do Java. Porque? <a href="https://stackoverflow.com/questions/11359187/why-not-use-java-util-logging">Porque não oras!</a></li>
  <li>Use SLF4J e escolha a implementação que você quiser.</li>
</ol>

<p>Com SFL4J você não precisa fazer quase nenhuma configuração em código, apenas instanciar em suas classes uma instância do logger.</p>

<h2 id="o-slf4j">O SLF4J</h2>

<p>O <strong>Simple Logging Facade for Java</strong> é uma biblioteca para abstrair log, assim você não precisariam ficar preso a uma implementação de biblioteca de log. Quer mudar a biblioteca de log? Só mudar umas linhas no <code class="language-plaintext highlighter-rouge">pom.xml</code>!</p>

<h3 id="configurando-o-slf4j">Configurando o SLF4J</h3>

<p>Para configurar o SLF4J e sua implementação, você irá precisar basicamente:</p>

<ol>
  <li>Adicionar a dependência <a href="https://mvnrepository.com/artifact/org.slf4j/slf4j-api">slf4j-api</a></li>
  <li>Adicionar uma implementação do SLF4J
    <ol>
      <li>Ou o <a href="https://logging.apache.org/log4j/2.x/">Log4J2</a></li>
      <li>Ou o <a href="http://logback.qos.ch/">Logback</a></li>
      <li>Ou outras <a href="http://www.slf4j.org/manual.html">implementações</a></li>
    </ol>
  </li>
  <li>Instanciar o log e deixar rastros.</li>
</ol>

<p>Basicamente não é necessário um arquivo de configuração, apesar de ser altamente recomendável!</p>

<p>Vou deixar aqui um exemplo não tão básico, pois usa o variáveis de ambiente para configurar o Log:</p>

<div data-gist="https://gist.github.com/vepo/82b64b6cc23bc5d85f0e05cdbc36603f"></div>

<p>Observe que na linha 21 e 22, estou usando as variáveis de ambiente <code class="language-plaintext highlighter-rouge">LOG_LEVEL</code> e <code class="language-plaintext highlighter-rouge">APPENDER</code>, com os respectivos valores padrão <code class="language-plaintext highlighter-rouge">DEBUG</code> e <code class="language-plaintext highlighter-rouge">STDOUT</code>. Assim essas configurações podem ser alteradas sem a necessidade de uma build! 😎</p>

<p>Agora para instanciar o log:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.slf4j.Logger</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.slf4j.LoggerFactory</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">MyClass</span> <span class="o">{</span>
	<span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">Logger</span> <span class="n">logger</span> <span class="o">=</span> <span class="nc">LoggerFactory</span><span class="o">.</span><span class="na">getLogger</span><span class="o">(</span><span class="nc">MyClass</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>

        <span class="kd">public</span> <span class="kt">void</span> <span class="nf">beHappy</span><span class="o">()</span> <span class="o">{</span>
            <span class="n">logger</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"Yeah!"</span><span class="o">);</span>
            <span class="c1">// being happy!</span>
        <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="níveis-de-log">Níveis de Log</h2>

<p>Um dos conceitos básicos do log é o Nível, ele gradua a importância da informação sendo registrada.</p>

<p>No SLF4J há os seguintes níveis: ERROR, WARN, INFO, DEBUG, TRACE. Sendo o TRACE o menos importante e o ERROR o mais importante.</p>

<p>É nível de log deve ser ajustado de acordo com a necessidade, por exemplo. Uma aplicação no ambiente de desenvolvimento deve rodar em DEBUG ou TRACE. Uma aplicação em ambiente de testes em INFO. Já uma aplicação em produção deve rodar em ERROR ou WARN.</p>

<p>Caso exista um bug em em produção e seja necessário pegar mais logs, a aplicação deve ser reiniciada para INFO.</p>

<p>Mas fique atento, mudar o nível de log pode impactar a performance da aplicação.</p>

<h2 id="dicas">Dicas</h2>

<p>É normal em desenvolvedores menos experientes, criar mensagens com contatenação de strings… Mas isso não é uma boa prática, porque em código sensíveis, pode impactar a performace.</p>

<p>Mas há duas maneiras de resolver isso com o SLF4J.</p>

<h3 id="usando-binding">Usando binding</h3>

<p>No SLF4J você pode resolver esse usando o binding. Você irá ver que as assinaturas dos métodos de log aceitam inúmeros parâmetros. Cara parametro será associado ao valor <code class="language-plaintext highlighter-rouge">{}</code> na mensagem de log. Assim, caso a mensagem não seja exibida por causa do nível de log, serão evitada as criações de algumas strings.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">logger</span><span class="o">.</span><span class="na">trace</span><span class="o">(</span><span class="s">"This is a trace message: ctx={} other-var={}"</span><span class="o">,</span> <span class="n">ctx</span><span class="o">,</span> <span class="n">otherVar</span><span class="o">);</span>
</code></pre></div></div>

<h3 id="verificando-nível-de-log">Verificando Nível de Log</h3>

<p>Caso a mensagem seja realmente complicada, e você queira formatar ela dignamente… Há outra saída…</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">logger</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"this is a debug message. If the level is bellow debug, it will not showed"</span><span class="o">);</span>
<span class="k">if</span><span class="o">(</span><span class="n">logger</span><span class="o">.</span><span class="na">isDebugEnabled</span><span class="o">())</span> <span class="o">{</span>
	<span class="n">logger</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"Some message formatted: value1="</span> <span class="o">+</span> <span class="n">value1</span> <span class="o">+</span> <span class="s">" ctxName="</span> <span class="o">+</span> <span class="n">ctx</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="não-use-chamadas-de-metodos">Não use chamadas de metodos</h3>

<p>Uma outra dica é nunca usar chamadas de métodos como parâmetro! Porque? Porque se o esse log não estiver habilitado, o método será chamado sempre. Deixe a JVM chamar o <code class="language-plaintext highlighter-rouge">toString</code>. Segue a vida….</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">logger</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"This is a log registry: ctxName={}"</span><span class="o">,</span> <span class="n">ctx</span><span class="o">.</span><span class="na">getName</span><span class="o">());</span> <span class="c1">// Wrong!</span>
<span class="n">logger</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"This is a log registry: ctx={}"</span><span class="o">,</span> <span class="n">ctx</span><span class="o">);</span>               <span class="c1">// Better now! Implement .toString correctly</span>
</code></pre></div></div>

<h1 id="benchmark">Benchmark</h1>

<p>Eu fiz um benchmark rápido, mas o objetivo dele não é mostrar qual implementação é melhor. <a href="http://logback.qos.ch/reasonsToSwitch.html">Há algumas razões</a> pra se escolher o logback, mas isso fica a sua escolha.</p>

<div class="github-repo">
    <h2>
        <img class="logo" src="/assets/images/plugins/github-logo.png" alt="GitHub logo" loading="lazy" />
        <img class="user" src="https://avatars.githubusercontent.com/u/353569?v=4" loading="lazy" />
        <span class="url">
            <a href="https://github.com/vepo">
                vepo
            </a> 
            /
            <a href="https://github.com/vepo/slf4j-benchmark">
                slf4j-benchmark
            </a>
        </span>
    </h2>
</div>

<p>O ponto central do meu benchmark é mostrar como o uso do nível de log impacta na performance.</p>

<p>No meu teste, são feitas 1.000.000 de chamadas ao log. Então…</p>

<p><img src="https://dev-to-uploads.s3.amazonaws.com/i/1rz55opvxepu63l54sqq.png" alt="Alt Text" /></p>

<p>Podemos ver, que se reduzirmos o nível de log podemos reduzir o tempo de execução! Isso porque:</p>

<ol>
  <li>Menos código é executado</li>
  <li>Não há gargalo para escrita (arquivo, console, TCP, etc…)</li>
</ol>

<h1 id="conclusão">Conclusão</h1>

<p>Nenhum software pode ser executado sem algum tipo de log. Para softwares mais sensíveis o log deve ser levado a sério.</p>

<p>Usando corretamente o log, você não terá impacto na performance, e ainda conseguirá mais informações para troubleshooting e bug fix!</p>

<p>😎</p>]]></content><author><name>{&quot;twitter&quot;=&gt;&quot;vepo&quot;, &quot;linkedin&quot;=&gt;&quot;https://www.linkedin.com/in/victorosorio/&quot;, &quot;picture&quot;=&gt;&quot;/assets/images/me.avif&quot;}</name></author><category term="Desenvolvimento de Software" /><category term="logging" /><category term="Java" /><category term="SLF4J" /><summary type="html"><![CDATA[Um Sênior precisa saber usar log... minto! Qualquer desenvolvedor precisar saber usar log. Aqui apresento como usar o SLF4J em Java.]]></summary></entry></feed>